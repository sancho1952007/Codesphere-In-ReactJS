prepare:
  steps:
    - name: Install Bun
      command: sudo npm i -g bun
    - name: Build Code
      command: bun run build
test:
  steps: []
run:
  steps:
    - name: Install Bun
      command: sudo npm i -g bun
    - name: Run the server
      command: bun backend-prod
